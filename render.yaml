services:
  - type: web
    name: ai-game-balancer
    runtime: docker
    plan: free
    healthCheckPath: /health
    autoDeployTrigger: checksPass
    envVars:
      - key: ENABLE_LIVE_AI
        value: "false"
